machine:
  python:
    version: 3.5.1
dependencies:
  pre:
    - pip install --upgrade pip
  override:
    - pip install -r requirements.txt
test:
  override:
      - bin/run-circleci-tests:
          timeout: 1800
          parallel: true
