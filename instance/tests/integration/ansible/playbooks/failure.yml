---
- name: Fail on purpose
  hosts: all
  tasks:
    - name: Update apt-get
      raw: "apt-get update -qq"

    - name: Install packages
      raw: "apt-get install -qq python-minimal"

    - name: Fail, propagating the error
      command: /bin/false
