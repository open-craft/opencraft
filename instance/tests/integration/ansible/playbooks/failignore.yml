---
- name: Fail and ignore on purpose
  hosts: all
  tasks:
    - name: Update apt-get
      raw: "apt-get update -qq"

    - name: Install packages
      raw: "apt-get install -qq python-minimal"

    - name: Fail, but ignore the error
      command: /bin/false
      ignore_errors: True
