const messages = {
  useOwnDomain: {
    defaultMessage: 'I want to use my own domain',
    description:
      'Link to direct user to page where they can set up their own domain.'
  }
};

export default messages;
